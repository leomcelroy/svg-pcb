{
  "name": "SVG-PCB",
  "version": "0.0.0",
  "scripts": {
    "start": "snowpack dev",
    "build": "snowpack build",
    "postbuild": "cp -r ./neil-components ./build"
  },
  "devDependencies": {
    "snowpack": "^3.8.8"
  }
}
