
/*

takes array

[
  [0, 0], // goes to point
  // or "0,0"
  [[5, 5], [10, 10]] // line from p0 to p1 
  // or "5,5 10,10"
  [[20, 20], [25, 25], [30, 40]] // quadratic bezier
  // or "20,20 25,25 30,40"
  [[20, 20], [25, 25], [30, 40], [40, 50]] // cubic bezier
  // or "20,20 25,25 30,40"
]
  
*/

export function path(pts, turtle) {

  return turtle;
}

export function line(start, end, turtle) {

  return turtle;
}

export function quadraticBezier(start, control, end, turtle) {
  
  return turtle;
}

export function cubicBezier(start, control0, control1, end, turtle) {

  return turtle;
}